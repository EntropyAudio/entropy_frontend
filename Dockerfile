FROM ubuntu:latest
EXPOSE 4200
EXPOSE 0.0.0.0
EXPOSE localhost

# docker build -t entropy_frontend .
# docker run -p 4200:4200 -v .:/code -it entropy_frontend /bin/bash


# export NODEJS_HOME=/usr/local/nodejs
# export PATH=$NODEJS_HOME/bin:$PATH

# cd code
# npm install
# ng serve --host 0.0.0.0
